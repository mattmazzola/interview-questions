import { findComplement } from './476_numbercompliment'

test('findComplement should return compliment', () => {
    expect(findComplement(5)).toBe(2)
})

test('findComplement should return compliment', () => {
    expect(findComplement(2)).toBe(1)
})

test('findComplement should return compliment', () => {
    expect(findComplement(6)).toBe(1)
})